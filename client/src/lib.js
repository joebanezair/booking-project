export const emptyContent={title:"",description:"",price:"",currency:"PHP",category:"General",coverImage:"",images:[],allowRatings:true,allowBookings:true,published:false};
export function money(value,currency="PHP"){try{return new Intl.NumberFormat(undefined,{style:"currency",currency}).format(Number(value||0));}catch{return `${currency} ${Number(value||0).toFixed(2)}`;}}
export function fileToDataUrl(file){return new Promise((resolve,reject)=>{if(!file)return resolve("");if(!["image/jpeg","image/png","image/webp","image/gif"].includes(file.type))return reject(new Error("Images must be JPEG, PNG, WebP, or GIF."));if(file.size>2*1024*1024)return reject(new Error("Each image must be 2 MB or smaller."));const reader=new FileReader();reader.onload=()=>resolve(String(reader.result||""));reader.onerror=()=>reject(new Error("Unable to read image."));reader.readAsDataURL(file);});}
