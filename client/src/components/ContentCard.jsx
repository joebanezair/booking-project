import { Link } from "react-router-dom"; import PriceDisplay from "./PriceDisplay.jsx"; import { RatingSummary } from "./StarRating.jsx";
export default function ContentCard({item,manage=false,onDelete,onToggle}){return <article className={manage?"panel admin-content-card":"public-content-card"}>{item.coverImage?<img className={manage?"admin-content-cover":""} src={item.coverImage} alt={item.title}/>:<div className={manage?"admin-content-cover content-placeholder":"content-placeholder"}>B</div>}<div className={manage?"admin-content-body":"public-content-card-body"}><div className="content-card-top"><span className="category-pill">{item.category}</span>{manage&&<><span className={`status ${item.visibility==="private"?"draft":"published"}`}>{item.visibility||"public"}</span><span className={item.published?"status published":"status draft"}>{item.published?"Published":"Draft"}</span></>}</div><h3>{item.title}</h3><p>{item.description}</p><PriceDisplay price={item.price} currency={item.currency}/>{!manage&&<RatingSummary averageRating={item.averageRating} ratingCount={item.ratingCount}/>} {manage&&<div className="row-actions wrap"><Link className="secondary button-link small-link" to={`/dashboard/services/${item._id}/edit`}>Edit</Link><button className="secondary" onClick={()=>onToggle(item)}>{item.published?"Unpublish":"Publish"}</button>{item.published&&item.visibility!=="private"&&<Link className="secondary button-link small-link" to={`/services/${item._id}`}>View</Link>}<button className="danger" onClick={()=>onDelete(item._id)}>Delete</button></div>}</div></article>;}
