export default function ContentGallery({coverImage,images=[],title}){const all=[coverImage,...images].filter(Boolean);if(!all.length)return <div className="content-placeholder large-placeholder">B</div>;return <div className="content-gallery"><img className="gallery-hero" src={all[0]} alt={title}/>{all.length>1&&<div className="gallery-thumbs">{all.slice(1).map((img,i)=><img src={img} alt={`${title} ${i+2}`} key={i}/>)}</div>}</div>;}
