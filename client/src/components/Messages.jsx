import { useEffect,useRef,useState } from "react"; import { api } from "../api.js"; import { getRealtimeSocket } from "../realtime.js";
export default function Messages({currentUser}){const [users,setUsers]=useState([]),[selected,setSelected]=useState(null),[conversation,setConversation]=useState([]),[body,setBody]=useState(""),[error,setError]=useState("");const selectedRef=useRef(null);useEffect(()=>{selectedRef.current=selected;},[selected]);useEffect(()=>{api.messages.users().then(setUsers).catch(e=>setError(e.message));const socket=getRealtimeSocket();if(!socket)return;const receive=message=>{if(String(message.sender)!==String(selectedRef.current?._id))return;setConversation(xs=>xs.some(x=>x._id===message._id)?xs:[...xs,message]);};socket.on("message:new",receive);socket.on("connect_error",e=>setError(`Real-time connection: ${e.message}`));return()=>{socket.off("message:new",receive);socket.off("connect_error");};},[]);
async function open(u){setSelected(u);try{const d=await api.messages.conversation(u._id);setConversation(d.messages);}catch(e){setError(e.message);}}
async function send(e){e.preventDefault();if(!selected||!body.trim())return;const m=await api.messages.send(selected._id,body);setConversation(xs=>[...xs,m]);setBody("");}
return <section id="messages" className="panel messages-panel"><div className="panel-title"><h2>Messages</h2></div>{error&&<p className="error">{error}</p>}<div className="messages-layout"><div className="user-list">{users.map(u=><button className={"user-row "+(selected?._id===u._id?"active":"")} key={u._id} onClick={()=>open(u)}><span className="avatar">{u.name[0]}</span><span><strong>{u.name}</strong><small>{u.email}</small></span></button>)}</div><div className="conversation">{!selected?<div className="empty-state"><p className="muted">Select a user to start a conversation.</p></div>:<><div className="conversation-header"><strong>{selected.name}</strong></div><div className="message-list">{conversation.map(m=><div key={m._id} className={"message-bubble "+(String(m.sender)===String(currentUser.id)?"mine":"theirs")}>{m.body}<small>{new Date(m.createdAt).toLocaleString()}</small></div>)}</div><form className="message-compose" onSubmit={send}><input value={body} onChange={e=>setBody(e.target.value)} placeholder="Write a message..."/><button className="primary-button">Send</button></form></>}</div></div></section>;}
