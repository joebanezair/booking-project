export function StarRating({value=0,size="md"}){return <span className={`stars ${size}`} aria-label={`${value} out of 5 stars`}>{[1,2,3,4,5].map(n=><span key={n} className={n<=Math.round(value)?"filled":""}>★</span>)}</span>;}
export function RatingInput({value=0,onChange,disabled=false}){return <div className="rating-input" role="group" aria-label="Rate from 1 to 5 stars">{[1,2,3,4,5].map(n=><button type="button" key={n} disabled={disabled} className={n<=value?"active":""} aria-label={`${n} star${n===1?"":"s"}`} aria-pressed={value===n} onClick={()=>onChange(n)}>★</button>)}</div>;}
export function RatingSummary({averageRating=0,ratingCount=0}){return <div className="rating-summary"><StarRating value={averageRating}/><strong>{averageRating||"0.0"}</strong><span>({ratingCount} {ratingCount===1?"rating":"ratings"})</span></div>;}
