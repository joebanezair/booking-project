import { useEffect,useState } from "react"; import { Link } from "react-router-dom"; import { api } from "../api.js"; import ContentCard from "../components/ContentCard.jsx";
export default function BrowsePage({user}){const [items,setItems]=useState([]),[error,setError]=useState("");useEffect(()=>{api.browse().then(setItems).catch(e=>setError(e.message));},[]);return <main className="public-page"><header className="public-top browse-top"><Link className="brand-row" to="/"><span className="brand-mark small">B</span><strong>BookFlow</strong></Link><div>{user?<Link className="primary-button button-link" to="/dashboard">Dashboard</Link>:<Link className="primary-button button-link" to="/login">Sign in</Link>}</div></header><section className="public-content-section browse-section"><div className="section-heading"><div><p className="eyebrow">DISCOVER</p><h1>Published content</h1><p className="muted">Browse public content from BookFlow creators.</p></div></div>{error&&<p className="error">{error}</p>}<div className="content-grid">{items.map(item=><Link key={item._id} to={`/content/${item._id}`} className="content-card-link"><ContentCard item={item}/></Link>)}</div></section></main>;}
