import { useEffect,useState } from "react"; import { useNavigate,useParams,Link } from "react-router-dom"; import { api } from "../api.js"; import ContentGallery from "../components/ContentGallery.jsx"; import PriceDisplay from "../components/PriceDisplay.jsx"; import ProfileAvatar from "../components/ProfileAvatar.jsx"; import { RatingInput,RatingSummary } from "../components/StarRating.jsx"; import CommentSection from "../components/CommentSection.jsx";
export default function PublicContentPage({user}){const {contentId}=useParams();const navigate=useNavigate();const [item,setItem]=useState(null),[error,setError]=useState("");async function load(){try{setItem(await api.publicContent(contentId));setError("");}catch(e){setError(e.message);}}useEffect(()=>{load();},[contentId]);
async function rate(v){if(!user)return navigate("/login");await api.ratings.set(contentId,v);await load();}async function removeRating(){await api.ratings.remove(contentId);await load();}async function addComment(comment){await api.comments.add(contentId,comment);await load();}async function editComment(id,comment){await api.comments.update(id,comment);await load();}async function deleteComment(id){await api.comments.remove(id);await load();}
if(error)return <main className="public-shell"><section className="public-card"><p className="error">{error}</p></section></main>;if(!item)return <main className="public-shell"><section className="public-card">Loading...</section></main>;
const isOwner=user&&String(user.id)===String(item.owner.id);return <main className="public-page"><header className="public-top"><Link className="brand-row" to="/"><span className="brand-mark small">B</span><strong>BookFlow</strong></Link></header><article className="content-detail"><ContentGallery coverImage={item.coverImage} images={item.images} title={item.title}/><div className="content-detail-body"><span className="category-pill">{item.category}</span><h1>{item.title}</h1><PriceDisplay className="content-price" price={item.price} currency={item.currency}/><p className="content-description">{item.description}</p>{item.allowBookings&&<div className="content-booking-callout"><div><strong>Interested in this content?</strong><p>Choose a convenient date and request a schedule with {item.owner.name}.</p></div><Link className="primary-button button-link" to={`/b/${item.owner.id}?content=${item._id}&service=${encodeURIComponent(item.title)}`}>Book a schedule</Link></div>}<div className="owner-strip"><ProfileAvatar profile={item.owner}/><div><strong>{item.owner.name}</strong>{item.owner.headline&&<small>{item.owner.headline}</small>}</div><Link className="secondary button-link" to={`/profile/${item.owner.username}`}>View profile</Link></div>{item.allowRatings&&<section className="rating-section"><p className="eyebrow">RATINGS</p><RatingSummary {...item.ratingSummary}/>{!isOwner&&(user?<div className="your-rating"><span>Your rating</span><RatingInput value={item.currentUserRating||0} onChange={rate}/>{item.currentUserRating&&<button className="link-button" onClick={removeRating}>Remove rating</button>}</div>:<button className="link-button" onClick={()=>navigate("/login")}>Sign in to rate</button>)}</section>}<CommentSection comments={item.comments||[]} currentUser={user} ownerId={item.owner.id} onAdd={addComment} onEdit={editComment} onDelete={deleteComment} onSignIn={()=>navigate("/login")}/></div></article></main>;}
