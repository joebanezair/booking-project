import {FiCheck,FiLogOut,FiMoon,FiSun} from "react-icons/fi";import AppLayout from "../components/AppLayout.jsx";
const options=[{value:"light",label:"Light",text:"Bright backgrounds and dark text.",icon:FiSun},{value:"dark",label:"Dark",text:"Dark backgrounds with reduced glare.",icon:FiMoon}];
export default function SettingsPage({user,onLogout,theme,onThemeChange}){return <AppLayout user={user} onLogout={onLogout}><header className="topbar"><div><p className="eyebrow">SETTINGS</p><h1>Appearance</h1><p className="muted">Choose how BookFlow looks on this device.</p></div></header><section className="panel settings-panel"><h2>Color theme</h2><p className="muted">Your choice is saved automatically.</p><div className="theme-grid">{options.map(({value,label,text,icon:Icon})=><button className={theme===value?"theme-option active":"theme-option"} onClick={()=>onThemeChange(value)} key={value}><Icon aria-hidden="true"/><span><strong>{label}</strong><small>{text}</small></span>{theme===value&&<FiCheck className="theme-check" aria-label="Selected"/>}</button>)}</div></section><section className="panel settings-panel"><h2>Account</h2><p className="muted">Sign out of your BookFlow account on this device.</p><button type="button" className="secondary signout-button settings-signout" onClick={onLogout}><FiLogOut aria-hidden="true"/><span>Sign out</span></button></section></AppLayout>;}
