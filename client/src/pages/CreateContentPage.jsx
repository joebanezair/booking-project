import { useNavigate,Link } from "react-router-dom"; import AppLayout from "../components/AppLayout.jsx"; import ContentForm from "../components/ContentForm.jsx"; import { emptyContent } from "../lib.js"; import { api } from "../api.js";
export default function CreateContentPage({user,onLogout}){const navigate=useNavigate();async function save(body){await api.content.create(body);navigate("/dashboard/content");}
return <AppLayout user={user} onLogout={onLogout}><header className="topbar"><div><p className="eyebrow">CMS</p><h1>Create content</h1><p className="muted">Add a new draft or publish it immediately.</p></div><Link className="secondary button-link" to="/dashboard/content">Back to content</Link></header><ContentForm initial={emptyContent} onSubmit={save} submitLabel="Create content"/></AppLayout>;}
