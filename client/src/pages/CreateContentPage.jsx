import { useNavigate,Link } from "react-router-dom"; import AppLayout from "../components/AppLayout.jsx"; import ContentForm from "../components/ContentForm.jsx"; import { emptyContent } from "../lib.js"; import { api } from "../api.js";
export default function CreateContentPage({user,onLogout}){const navigate=useNavigate();async function save(body){await api.content.create(body);navigate("/dashboard/services");}
return <AppLayout user={user} onLogout={onLogout}><header className="topbar"><div><p className="eyebrow">SERVICES</p><h1>Post a service</h1><p className="muted">Save a draft, publish publicly, or keep it private.</p></div><Link className="secondary button-link" to="/dashboard/services">Back to services</Link></header><ContentForm initial={emptyContent} onSubmit={save} submitLabel="Post service"/></AppLayout>;}
